import React, { useContext, ReactElement, useState } from 'react';

interface SidebarArtboardStylesProps {
  artboard: paper.Group;
  path: string;
  depth: number;
}

const SidebarArtboardStyles = (props: SidebarArtboardStylesProps): ReactElement => {

  return (
    <div>
    </div>
  );
}

export default SidebarArtboardStyles;